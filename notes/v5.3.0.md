
### Fixed
- **Live-PostgreSQL 500s on workforce/user-profile/dashboard-overview**: the ten
  Phase-45 `user_journey_cache` columns finally got their `ALTER TABLE`
  migrations (deployments created before Phase 45 were missing them all).
- **Updates page crash** ("cfg is not defined") introduced with the v5.2.0
  Release-source panel.

### Changed
- **Dependency refresh across the platform** (validated against the full test
  suite): FastAPI 0.139 (clears the long-ignored starlette CVEs), uvicorn 0.49,
  SQLAlchemy 2.0.51, pydantic 2.13, redis 8, aiofiles 25; agent cryptography 49;
  **React 19**, vite 8, TypeScript 6, node 26 builder; pytest 9 +
  pytest-asyncio 1.4; GitHub Actions v7/v6 line-up.
- UAM query performance: targeted JSON field extraction instead of full-payload
  casts; explicit column lists instead of `SELECT *` in discovered-users and
  insider-risk paths.
- Historical project docs archived; stale evidence artifacts removed.

### Operations
- e2e test dependencies split into `server/requirements-e2e.txt` (installing
  playwright into the unit-test venv made `test_e2e` run instead of skip).
- Dependabot: python docker-image bumps ignored (Python 3.12 wheel ceiling);
  branch auto-delete enabled on the repo.


